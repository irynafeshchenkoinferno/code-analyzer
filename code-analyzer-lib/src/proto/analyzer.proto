syntax = "proto3";

package ai;

service AiAgentService {
  rpc GetResponse(AiRequest) returns (AiResponse);

  rpc StreamResponse(AiRequest) returns (stream AiResponse);
}

message AiRequest {
  string prompt = 1;
}

message AiResponse {
  string text = 1;
}
